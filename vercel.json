{
    "builds": [{
        "src": "contrato/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "contrato/wsgi.py"
        }
    ]
}